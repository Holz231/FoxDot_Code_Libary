def once(self):
    ''' play sound once '''
    self.dur = 1/16 # fast dur allows instant feedback
    self.amp = var([1, 0], [self.dur, inf], start=Clock.now())
    return self
    
Player.once = once

# plays the sound once every time you evaluate the code
k1 >> play('x').once()
